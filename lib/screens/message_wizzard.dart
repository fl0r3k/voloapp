import 'package:flutter/material.dart';

class MessageWizardScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('Kreator Wiadomości'),
    );
  }
}
